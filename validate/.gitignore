# Where this machine keeps the DATs, and what a run leaves behind.
data-dir
*.log

# A run seeds and edits profiles here. The suite's own scratch profile
# is committed, because the checks switch to it rather than write to
# whatever profile the character happens to read.
profiles/*
!profiles/Validate.json

# A run writes each character's loot ledger here.
taken/
