[package]
name = "ac-plugin"
description = "The plugin interface of the acswarm client: read the game, act in it, draw UI"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
ac-bus = { path = "../ac-bus" }
ac-client = { path = "../ac-client" }
ac-vendor = { path = "../ac-vendor" }
ac-formats = { path = "../ac-formats" }
ac-net = { path = "../ac-net" }
ac-world = { path = "../ac-world" }
ac-scene = { path = "../ac-scene" }
egui = "0.36"
serde.workspace = true
serde_json = "1"
glam = "0.30"
tracing.workspace = true

[dev-dependencies]
# The data directory for tests that read the archives.
ac-dat = { path = "../ac-dat" }
